{{- if .Values.tmdb.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ncore-tmdb.fullname" . }}
  labels:
    {{- include "ncore-tmdb.labels" . | nindent 4 }}
spec:
  ingressClassName: {{ .Values.tmdb.ingress.className }}
  rules:
    - host: {{ .Values.tmdb.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "ncore-tmdb.fullname" . }}
                port:
                  number: {{ .Values.tmdb.service.port }}
{{- end }}
